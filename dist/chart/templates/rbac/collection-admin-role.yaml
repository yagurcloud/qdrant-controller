{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "yagur-controllers.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "yagur-controllers.resourceName" (dict "suffix" "collection-admin-role" "context" $) }}
rules:
- apiGroups:
  - qdrant.yagur.io
  resources:
  - collections
  verbs:
  - '*'
- apiGroups:
  - qdrant.yagur.io
  resources:
  - collections/status
  verbs:
  - get
{{- end }}
