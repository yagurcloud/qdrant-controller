{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: yagur-controllers
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "qdrant-controller.resourceName" (dict "suffix" "collection-viewer-role" "context" $) }}
rules:
- apiGroups:
  - qdrant.yagur.io
  resources:
  - collections
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - qdrant.yagur.io
  resources:
  - collections/status
  verbs:
  - get
{{- end }}
